import React from "react";
import { ReviewOrder } from "../../../components/Order/ReviewOrder";

const ReviewOrderPage = ({ id }) => {
  return <ReviewOrder id={id} />;
};

export default ReviewOrderPage;
